{% spaceless %}
<?xml version="1.0" encoding="UTF-8"?>
<urlset
  xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:news="http://www.google.com/schemas/sitemap-news/0.9">
  {% for url in urlset %}
  <url>
    <loc>{{ url.location }}</loc>
    {% if url.lastmod %}<lastmod>{{ url.lastmod|date:"Y-m-d" }}</lastmod>{% endif %}
    {% if url.changefreq %}<changefreq>{{ url.changefreq }}</changefreq>{% endif %}
    {% if url.priority %}<priority>{{ url.priority }}</priority>{% endif %}
    <news:news>
      <news:publication>
        <news:name>{{ publication_name }}</news:name>
        <news:language>{{ sitemap_language }}</news:language>
      </news:publication>
      {% if url.item.date_available %}<news:publication_date>{{ url.item.date_available|date:"Y-m-d" }}</news:publication_date>{% endif %}
      <news:title>{{ url.item.title|striptags }}</news:title>
      {% if url.item.tags %}<news:keywords>{{ url.item.tags }}</news:keywords>{% endif %}
    </news:news>
  </url>
  {% endfor %}
</urlset>
{% endspaceless %}
